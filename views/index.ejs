<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Try Curl</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        h2 {
            background-color:  rgb(107, 172, 10);
            padding: 5px 0;
            text-align: center;
        }

        form {
            margin: 10px auto;
            display: flex;
            align-items: center;
            width: 90vw;
            max-width: 600px;
        }

        input {
            width: 90%;
            height: 45px;
            border-radius: 5px;
            padding-left: 5px;
        }

        button {
            font-weight: bold;
            cursor: pointer;
            margin-left: 2%;
            border-radius: 5px;
            border: 3px solid black;
            color: white;
            height: 30px;
            width: 30px;
            border: none;
            outline: none;
            font-size: 1.2em;
            background-color: rgb(107, 172, 10);
        }
    </style>
</head>

<body>
    <h2>Try Curl</h2>
    <form action="/main" method="post">
        <input type="text" name="command" placeholder="type the command here .." id="command">
        <button type="submit"> > </button>
    </form>
</body>

</html>